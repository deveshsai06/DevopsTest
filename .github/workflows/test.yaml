name: Run HelloWorld Script
on: [push, pull_request, workflow_dispatch]
jobs:
  execute-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      
      - name: Verify hello.sh exists
        run: |
          if [ -f "hello.sh" ]; then
            echo "✅ hello.sh found!"
            echo "Permissions: $(ls -la hello.sh)"
          else
            echo "❌ hello.sh not found!"
            exit 1
          fi
      
      - name: Make script executable
        run: chmod +x hello.sh
      
      - name: Run hello.sh
        run: ./helloworld.sh
      
      - name: Display script content (for logs)
        run: |
          echo "📄 Content of hello.sh:"
          cat hello.sh
          echo "─────────────────────────"
